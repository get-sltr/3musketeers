# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_KEY=              # For server-side admin operations (keep secret)

# Supabase Service Key (KEEP SECRET - Server-side only)
SUPABASE_SERVICE_KEY=eyJhbGc...

# Security - Webhook Secrets
SUPABASE_WEBHOOK_SECRET=your-supabase-webhook-secret-from-dashboard
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# Security - CSRF Protection
# Generate with: openssl rand -hex 32
CSRF_SECRET=generate-a-random-secret-key-here

# Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1...
NEXT_PUBLIC_MAPBOX_SLTR_STYLE=mapbox://styles/sltr/cmhum4i1k001x01rlasmoccvm

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SOCKET_URL=            # Backend socket URL (e.g., https://your-backend.railway.app)

# Internal API Key (Required for push notifications)
# Generate a secure random string: openssl rand -hex 32
# Must match between frontend and backend deployments
INTERNAL_API_KEY=your-secure-internal-api-key

# EROS Backend (AI Matching)
NEXT_PUBLIC_EROS_BACKEND_URL=https://eros-backend-production.up.railway.app
NEXT_PUBLIC_DEV_BACKEND_URL=http://localhost:3001

# LiveKit (Video Calls)
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret
NEXT_PUBLIC_LIVEKIT_URL=wss://your-livekit-server.livekit.cloud

# Stripe (Payments)
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Sentry (Error Tracking)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id
SENTRY_ORG=your-org-name
SENTRY_PROJECT=your-project-name
SENTRY_AUTH_TOKEN=your-auth-token
